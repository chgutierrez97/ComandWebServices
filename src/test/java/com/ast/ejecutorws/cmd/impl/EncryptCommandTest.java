package com.ast.ejecutorws.cmd.impl;

import org.junit.Test;

import com.ast.ejecutorws.args.ArgsData;
import com.ast.ejecutorws.cmd.CommandWS;
import com.ast.ejecutorws.cmd.CommandWSException;
import com.google.common.base.Optional;

public class EncryptCommandTest {
/*
	@Test
	public void testExecute() throws CommandWSException {
		ArgsData inputData = new ArgsData();
		inputData.setPassword(Optional.of("accusys123"));
		CommandWS command = new EncryptCommand(inputData);
		command.execute();
		// assertEquals(0, command.execute());
	}

	@Test(expected = CommandWSException.class)
	public void testContainsRequiredParameters() throws CommandWSException {
		ArgsData inputData = new ArgsData();
		inputData.setEndpoint(Optional.of("asdas"));
		new EncryptCommand(inputData).execute();
	}*/

}
