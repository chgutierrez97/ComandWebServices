package com.ast.ejecutorws.cmd.impl;

import java.io.IOException;

import org.junit.Test;
import org.mockito.Mockito;

import com.ast.ejecutorws.cmd.CommandWSException;

public class HelpCommandTest {
/*
	@Test
	public void testExecute() throws IOException, InterruptedException, CommandWSException {
		HelpCommand helpCommand = Mockito.spy(HelpCommand.class);
		helpCommand.execute();
		Mockito.verify(helpCommand, Mockito.times(6)).log(Mockito.anyString());
	}*/

}
