package com.ast.ejecutorws.args;

import java.io.IOException;

import org.junit.Assert;
import org.junit.Test;
import org.kohsuke.args4j.CmdLineException;

public class ArgumentParserTest {
/*
	@Test
	public void testParseArguments() throws CmdLineException {
		ArgsData inputData = ArgumentParser
				.parseArguments(new String[] { "-c", "SOAP", "-e", "http://123...", "-r", "<soapenv>...</soapenv>", "-a", "http://action" });
		Assert.assertEquals("http://123...", inputData.getEndpoint().get());
		Assert.assertEquals("<soapenv>...</soapenv>", inputData.getRequest().get());
		Assert.assertEquals("http://action", inputData.getSoapAction().get());
	}

	@Test
	public void testParseArgumentsByAlias() throws CmdLineException {
		ArgsData inputData = ArgumentParser.parseArguments(
				new String[] { "--command", "SOAP", "--endpoint", "http://123...", "--request", "<soapenv>...</soapenv>", "--soapAction", "http://action" });
		Assert.assertEquals("http://123...", inputData.getEndpoint().get());
		Assert.assertEquals("<soapenv>...</soapenv>", inputData.getRequest().get());
		Assert.assertEquals("http://action", inputData.getSoapAction().get());
	}

	@Test
	public void testPrintUsage() throws IOException {
		ArgumentParser.printUsage(System.out);
	}

	// @Test
	// public void testHeaders() throws IOException {
	// ArgsData inputData = ArgumentParser.parseArguments(new String[] { "--endpoint", "http://123...", "--soap", "<soapenv>...</soapenv>", "--soapAction",
	// "http://action" , "-h","asd","-h","123"});
	// Assert.assertEquals(2, inputData.getHeaders().length);
	// }

	@Test
	public void testFileInput() throws IOException {
		ArgsData inputData = ArgumentParser.parseArguments(new String[] { "-c", "SOAP", "--endpoint", "http://123...", "--request",
				"C:\\Users\\franco.milanese\\Desktop\\curl_test\\request.xml", "--soapAction", "http://action" });
		Assert.assertEquals("C:\\Users\\franco.milanese\\Desktop\\curl_test\\request.xml", inputData.getRequest().get());
	}*/

}
